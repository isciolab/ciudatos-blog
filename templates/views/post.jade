extends ../layouts/default

blog lang
	//- ul.list-inline#lang
	//- 	li: a(href='/en')
	//- 		small EN
	//- 	li: a(href='/es')
	//- 		small ES

block content
	.container.margin-top: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
		article
			p: a(href='/blog') &larr; #{__('post.backToBlog')}
			hr
			if !data.post
				h2 Invalid Post.
			else
				header
					h1= data.post.title
					h5 #{__('common.published')}  
						if data.post.publishedDate
							| #{data.post._.publishedDate.format('YYYY-MM-DD')},
						if data.post.categories && data.post.categories.length
							| #{__('common.category')}  
							each cat, cat_i in data.post.categories
								a(href='/blog/' + cat.key)= cat.name
								if cat_i < data.post.categories.length - 1
									| , 
								else
									|  
						if data.post.author
							| #{__('common.by')}  #{data.post.author.name.first}
				.post
					if data.post.image.exists
						.image-wrap: img(src=data.post._.image.fit(750,450)).img-responsive
					!= data.post.content.full
